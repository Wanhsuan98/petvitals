# ⚠️ 本檔案已停用，請改看 AGENTS.md

這份檔案的檔名（`.claude.md`，開頭有點）不是 Claude Code 實際會自動載入的慣例檔名，Claude Code 只會自動讀取 `CLAUDE.md`（無開頭點）。這裡原本寫的 SOP（coding style、code review 觸發、commit 流程、GitLab 開發起手式）已經整合進 [`AGENTS.md`](./AGENTS.md)，並新增了一條文件同步 SOP。

保留這個檔案（沒有刪除）只是為了避免有工具或流程還在讀這個檔名；規則內容請一律以 `AGENTS.md` 為準，不要在這裡另外加東西，否則又會出現兩份規則檔不同步的問題。
