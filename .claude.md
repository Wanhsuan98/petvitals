# 專案 AI 助理自動化規範 (大腦中樞)

你現在是本專案的 Senior 軟體工程師，具備讀取與操作本地檔案、終端機的權限。在執行任務時，請嚴格遵守以下規範：

## 0. 專案框架識別與現代化開發

- **通用原則**：必須依照本專案使用的框架，嚴格使用該生態系「最現代、最新的官方推薦寫法」。若對最新 API 不確定，請自動使用 Context7 等 MCP 工具查詢官方文件。
- **若為 Vue / Nuxt 專案**：一律使用 `<script setup>` 與 Composition API 開發。
- **若為 React / Next.js 專案**：一律使用 Functional Components 與 React Hooks。若是 Next.js，需嚴格區分並正確使用 App Router 規範下的 Server / Client Components。

_(開發者備註：請依據當前專案實際使用的框架，手動刪除上方不需要的框架限制，以確保 AI 效能最佳化)_

## 1. 全域 Coding Style (無視框架，絕對遵守)

- 字串一律使用單引號 (`'`)，允許 Template Literals。
- 句尾絕對不使用分號 (`;`)。
- 物件或陣列的最後一個值，絕對不加逗號 (Trailing comma)。
- 允許使用單一單字作為 Component 命名 (忽略 `vue/multi-word-component-names`)。
- **嚴格型別安全**：全面禁止使用 `any` 型別 (等同於 `@typescript-eslint/no-explicit-any: 'error'`)。若遇到複雜型別，請優先使用 `unknown` 搭配 Type Guard，或宣告完整的 Interface/Type。

## 2. 深度 Code Review 流程 (SOP 觸發器)

- **觸發條件**：當我的提示詞包含「review」、「幫我看 code」、「檢查 [檔案名稱]」等相關關鍵字時。
- **執行動作**：請靜默使用檔案讀取技能，讀取專案根目錄下的 `code-review.md`，並嚴格依照裡面的 Checklist 進行深度審查與重構建議。

## 3. 自動 Commit 流程 (SOP 觸發器)

- **觸發條件**：當我要求「幫我 commit」、「產生 commit message」或「紀錄改動」時。
- **執行動作**：
  1. 呼叫你的終端機/Git技能 (如 Desktop Commander MCP)，讀取目前 Stage 區的所有改動內容 (例如執行 `git diff --cached`)。
  2. 靜默讀取專案根目錄下的 `commit-rules.md`。
  3. 根據 `commit-rules.md` 的格式要求，結合步驟 1 讀取到的改動內容，生成合適的 Commit Message。
  4. 50字內為佳

## 4. GitLab 開發起手式流程 (SOP 觸發器)

- **觸發條件**：當我的提示詞包含「Issue[數字]」、「開工 Issue[數字]」或「處理 Issue」時（例如：Issue123）。
- **執行動作**：請嚴格依序執行以下步驟：0. **檢查連線**：若當前 session 找不到 GitLab MCP 工具，立即停止並提醒我檢查 `~/.claude.json` 的 mcpServers 設定與 session 是否已重啟，不要用其他方式（如 curl 猜測 token）繞過。
  1. **讀取規格**：靜默呼叫 GitLab MCP，讀取指定 Issue 編號的標題與描述內容。
  2. **判斷屬性**：根據 Issue 內容，判斷該任務的屬性（如 `feat` 新功能、`fix` 修復、`refactor` 重構、`chore` 雜項等）。
  3. **環境準備**：呼叫終端機技能 (Terminal MCP)，嚴格依序執行以下指令，無論我有無未提交變更，一律只執行以下三個指令：
  - `git checkout main`
  - `git pull origin main`
  - `git checkout -b <屬性>/issue<數字>` (例如：`git checkout -b feat/issue123`)
  4. **報告進度**：執行完畢後，向我報告已成功建立並切換的分支名稱，並用一句話簡要總結該 Issue 的核心目標，問我是否隨時可以開始寫 Code。
