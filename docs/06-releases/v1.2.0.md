# v1.2.0

Status: In Development (spec/design 已完成，尚未動工實作)

## Goals

讓照護不因單一人力而中斷：開放多照護者協作，並把主動提醒排程做進日常使用流程。

## Changes

### Caregivers

- [ ] 新增 `pet_caregivers` 資料表與 RLS 政策（owner OR accepted caregiver）
- [ ] `settings/caregivers` 邀請/移除管理頁
- [ ] 邀請流程（email 比對，沿用現有登入機制）
- [ ] 訂閱狀態綁定：第 2 位起協作者需要訂閱中才能存取

詳見 [../01-requirements/caregivers.md](../01-requirements/caregivers.md)

### Notification

- [ ] 新增 `reminder_schedules`、`push_subscriptions` 資料表
- [ ] VAPID 金鑰產生與環境變數設定
- [ ] `settings/reminders` 排程設定頁
- [ ] `api/push/subscribe`、`api/push/unsubscribe`
- [ ] `api/cron/send-reminders` + 排程觸發（需先查證 Vercel Cron 頻率限制）
- [ ] Service Worker 補上 `push` 事件監聽

詳見 [../01-requirements/notification.md](../01-requirements/notification.md)

### Dashboard（順帶交付，已完成）

- [x] 四個分頁補上 `loading.tsx` 骨架畫面，改善底部 Tab 切換時的空白卡頓感

## Technical

- [ ] RLS 政策變更需要涵蓋 `daily_care_logs`、`blood_tests`（見 [../04-architecture/backend.md](../04-architecture/backend.md)）
- [ ] 需要事先查證 Vercel Cron 在目前方案下的執行頻率上限是否足夠

## Breaking Changes

None（純新增功能，不影響現有免費功能行為）

## Migration

- 新增 3 張表（`pet_caregivers`、`reminder_schedules`、`push_subscriptions`），需依本專案慣例明確 `grant` 給 `authenticated`/`service_role`（見 [../04-architecture/backend.md](../04-architecture/backend.md) 的維運提醒）
- 修改 `daily_care_logs`、`blood_tests` 的既有 RLS 政策

## Not Included (documented but deferred)

- 多隻貓咪管理：架構前向相容設計已記錄於 [../00-product/roadmap.md](../00-product/roadmap.md)，本次不實作
