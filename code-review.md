# 專案 Code Review 健檢流程

這個工作流將引導 Agent 對使用者目前的程式碼進行深度的 Code Review。請嚴格按照以下六個步驟執行，並在每一步驟完成後才進入下一步。

## Step 1: 靜態分析與型別檢查 (TypeScript)

- **嚴格型別**：檢查是否完全沒有使用 `any` 或 `@ts-ignore`（若有複雜型別，應要求明確定義 Interface 或 Type）。
- **命名語意**：變數與函式命名是否具備清晰的語意？布林值是否建議以 `is/has/should` 開頭？

## Step 2: 單一職責與元件架構 (SRP)

- **元件拆分**：檢查該檔案是否過於龐大（如超過 300 行）？邏輯與 UI 是否混雜？
- **抽離建議**：請評估是否需要將部分商業邏輯抽離為 composables/hooks，或將龐大的 UI 拆分為更小的子元件。

## Step 3: 現代框架思維與副作用 (Side Effects)

- **框架心智模型**：確認是否有混用到過期的框架思維（例如在 Vue 3 中使用 Vue 2 Options API 思維，或在 React 中濫用 Class Component）。
- **效能與渲染**：檢查是否有不必要的 Re-render 風險，以及計算屬性（如 `computed` / `useMemo`）是否被正確運用來快取昂貴的計算。
- **副作用管理**：嚴格檢查 `watch` / `useEffect` 等副作用是否有正確的依賴陣列，以及是否正確清理 (Cleanup)，避免 Memory Leak 或無窮迴圈。

## Step 4: 錯誤處理與資安防護 (Security)

- **非同步錯誤處理**：API 請求與 Promise 操作是否都有完整的 `try-catch` 錯誤捕捉機制？是否有處理 Loading 狀態與錯誤 Fallback UI？
- **資安防護 (XSS)**：嚴格檢查是否濫用字串注入（如 Vue 的 `v-html` 或 React 的 `dangerouslySetInnerHTML`）。若必須使用，是否具備防範 XSS 攻擊的過濾機制 (Sanitization)？

## Step 5: UI 與無障礙 (a11y) 規範

- **無障礙標籤**：檢查按鈕、表單輸入框、圖片等互動元素，是否具備正確的 `aria-label`、`role` 或 `alt` 等無障礙標籤。
- **排版原則**：確認是否遵循專案的無分號 (`;`) 與單引號 (`'`) 排版原則。

## Step 6: 產出審查報告

綜合以上步驟，以繁體中文輸出一份專業的審查報告，格式必須嚴格遵循以下結構：

1. **🟢 表現優良之處**：簡短肯定寫得好的架構或邏輯。
2. **🔴 必須修正的致命錯誤**：指出會導致應用程式崩潰、資安漏洞或嚴重型別錯誤的 Bug。
3. **🟡 建議優化項目**：效能提升、元件拆分建議或更優雅的現代寫法。
4. **💻 修正後的程式碼建議**：直接提供修改後的 Code Block 供使用者參考（請務必遵守專案的單引號與無分號排版規範）。
